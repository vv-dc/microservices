apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: server
spec:
  selector:
    matchLabels:
      app: server
  namespaceSelector:
    any: true
  endpoints:
    - port: server
      interval: 30s
