apiVersion: v1
kind: Service
metadata:
  name: server-service
  labels:
    app: server
spec:
  type: ClusterIP
  selector:
    app: server
  ports:
    - name: server
      port: 3000
      targetPort: 3000
